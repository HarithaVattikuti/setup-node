name: Licensed

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  call-licensed:
    name: Licensed
    uses: priyagupta108/reusable-workflows/.github/workflows/basic-validation.yml@update-licensed-tool
